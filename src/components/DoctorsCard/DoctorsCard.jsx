import { FaEye } from "react-icons/fa";

const DoctorsCard = ({doctor}) => {
    const {experience_years, hosptal, name, specialty, location, rating} = doctor
    return (
        <div>
            
        </div>
    );
};

export default DoctorsCard;